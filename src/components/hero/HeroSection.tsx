import {
  HeroSectionContainer,
  HeroBackgroundImage,
  HeroBackgroundGradient,
  HeroTextSection,
  HeroSportsCarSection,
} from "./";

function HeroSection() {
  return (
    <HeroSectionContainer>
      <HeroBackgroundImage />
      <HeroTextSection />
      <HeroSportsCarSection />
      <HeroBackgroundGradient />
    </HeroSectionContainer>
  );
}

export default HeroSection;
